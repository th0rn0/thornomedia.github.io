---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  eyebrow: string;
  subtitle: string;
  images: { src: ImageMetadata; alt: string }[];
  primaryCta: { label: string; href: string };
  secondaryCta: { label: string; href: string };
}

const { eyebrow, subtitle, images, primaryCta, secondaryCta } = Astro.props;

// Crossfade timing: each image holds, then dissolves into the next.
// Delay is offset by -FADE so the first image is already fully faded in at load
// (no blank flash on first paint). Keyframe stops are percentages of TOTAL, which
// depends on images.length, so they're computed here rather than hardcoded.
const FADE = 1.5;
const HOLD = 6;
const SLOT = FADE + HOLD;
const TOTAL = images.length * SLOT;
const delayFor = (i: number) => i * SLOT - FADE;
const pct = (s: number) => ((s / TOTAL) * 100).toFixed(3);
const crossfadeKeyframes = `
  @keyframes crossfade {
    0% { opacity: 0; }
    ${pct(FADE)}% { opacity: 1; }
    ${pct(FADE + HOLD)}% { opacity: 1; }
    ${pct(FADE + HOLD + FADE)}% { opacity: 0; }
    100% { opacity: 0; }
  }
`;
---

<header class="hero">
  {images.length > 1 && <style set:html={crossfadeKeyframes} />}
  {
    images.map(({ src, alt }, i) => (
      <Image
        class={images.length > 1 ? 'hero-bg hero-bg--animated' : 'hero-bg'}
        src={src}
        alt={alt}
        widths={[640, 1024, 1600, 1920]}
        sizes="100vw"
        priority={i === 0}
        loading={i === 0 ? 'eager' : 'lazy'}
        style={
          images.length > 1
            ? `animation-duration:${TOTAL}s; animation-delay:${delayFor(i)}s;`
            : undefined
        }
      />
    ))
  }
  <div class="hero-scrim"></div>
  <div class="hero-frame"></div>
  <div class="lockon"><span class="tl"></span><span class="tr"></span><span class="bl"></span><span class="br"></span></div>
  <div class="wrap hero-content">
    <p class="eyebrow hero-eyebrow">{eyebrow}</p>
    <h1><slot /></h1>
    <p class="hero-sub">{subtitle}</p>
    <div class="hero-actions">
      <a class="btn btn-primary" href={primaryCta.href}>{primaryCta.label}</a>
      <a class="btn hero-btn-secondary" href={secondaryCta.href}>{secondaryCta.label}</a>
    </div>
  </div>
</header>

<style>
  .hero {
    position: relative;
    height: calc(100vh - var(--nav-height));
    height: calc(100dvh - var(--nav-height));
    min-height: 480px;
    display: flex;
    align-items: center;
    overflow: hidden;
    background: #14181a;
  }
  .hero :global(.hero-bg) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .hero :global(.hero-bg--animated) {
    opacity: 0;
    animation-name: crossfade;
    animation-timing-function: ease-in-out;
    animation-iteration-count: infinite;
  }
  @media (prefers-reduced-motion: reduce) {
    .hero :global(.hero-bg--animated) { animation: none; opacity: 0; }
    .hero :global(.hero-bg--animated:first-child) { opacity: 1; }
  }
  .hero-scrim {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(0deg, rgba(11, 13, 15, 0.92) 0%, rgba(11, 13, 15, 0.25) 46%, rgba(11, 13, 15, 0.15) 70%);
  }
  .hero-frame {
    position: absolute;
    inset: 22px;
    border: 1px solid rgba(255, 255, 255, 0.14);
    pointer-events: none;
  }
  .lockon { position: absolute; inset: 22px; pointer-events: none; }
  .lockon span {
    position: absolute;
    width: 26px;
    height: 26px;
    border-color: var(--accent);
    opacity: 0;
    animation: lockon-in 1.1s cubic-bezier(0.2, 0.7, 0.3, 1) 0.15s forwards;
  }
  .lockon .tl { top: -1px; left: -1px; border-top: 2px solid; border-left: 2px solid; }
  .lockon .tr { top: -1px; right: -1px; border-top: 2px solid; border-right: 2px solid; }
  .lockon .bl { bottom: -1px; left: -1px; border-bottom: 2px solid; border-left: 2px solid; }
  .lockon .br { bottom: -1px; right: -1px; border-bottom: 2px solid; border-right: 2px solid; }
  @keyframes lockon-in {
    from { opacity: 0; transform: scale(1.6); }
    to { opacity: 1; transform: scale(1); }
  }
  @media (prefers-reduced-motion: reduce) {
    .lockon span { animation: none; opacity: 1; }
  }

  .hero-content { position: relative; z-index: 2; width: 100%; }
  .hero-eyebrow { color: rgba(255, 255, 255, 0.62); margin-bottom: 18px; }
  .hero h1 {
    color: #f4f1ec;
    font-size: clamp(2.2rem, 6vw, 4.6rem);
    line-height: 0.98;
    letter-spacing: -0.01em;
    max-width: 15ch;
  }
  .hero-sub {
    color: rgba(255, 255, 255, 0.72);
    font-size: 1.05rem;
    max-width: 46ch;
    margin-top: 20px;
    line-height: 1.55;
  }
  .hero-actions { display: flex; gap: 14px; margin-top: 32px; flex-wrap: wrap; }
  .hero-btn-secondary { background: transparent; color: #f4f1ec; border-color: rgba(255, 255, 255, 0.35); }
  .hero-btn-secondary:hover { border-color: #f4f1ec; }
</style>
